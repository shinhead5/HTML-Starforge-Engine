{
  "name": "@starforge/sandbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@starforge/engine": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "vite": "^5.4.2"
  }
}
